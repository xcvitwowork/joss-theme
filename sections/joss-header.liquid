{%- render 'header' -%}
{%- render 'mobile-drawer' -%}

{% schema %}
{
  "name": "Header",
  "settings": [
    { "type": "paragraph", "content": "Configure logo, social links and other global settings in Theme Settings." }
  ]
}
{% endschema %}
