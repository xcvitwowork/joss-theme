{{ section.settings.custom_liquid }}
{% schema %}
{
  "name": "Custom Liquid",
  "tag": "div",
  "settings": [
    { "type": "liquid", "id": "custom_liquid", "label": "Custom Liquid / HTML / JS" }
  ],
  "presets": [{ "name": "Custom Liquid" }]
}
{% endschema %}
